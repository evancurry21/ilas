{#
  paragraph--accordion-item.html.twig
  Used by the "Accordion Item" paragraph type.

  Fields:
    - field_accordion_title (title displayed in the accordion header)
    - field_accordion_body (content displayed when the accordion is expanded)
#}

{# This template doesn't render any HTML directly, as the parent accordion_section template handles display #}
{# It's recommended to have this file for customization purposes if needed later #}

{{ content }}