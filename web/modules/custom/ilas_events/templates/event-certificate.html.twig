{#
/**
 * @file
 * Default theme implementation for event certificate.
 *
 * Available variables:
 * - event: The event entity.
 * - participant: The participant record.
 * - contact: The contact information.
 * - credits: CLE credits (if applicable).
 * - date_issued: Date the certificate was issued.
 * - certificate_number: Unique certificate number.
 * - type: Type of certificate.
 *
 * @ingroup themeable
 */
#}
<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>{{ type }} - {{ contact.display_name }}</title>
  <style>
    @page {
      size: letter landscape;
      margin: 0;
    }
    
    body {
      margin: 0;
      padding: 40px;
      font-family: 'Georgia', serif;
      background: white;
      color: #333;
    }
    
    .certificate {
      border: 20px solid #004080;
      padding: 60px;
      text-align: center;
      position: relative;
      min-height: 500px;
    }
    
    .certificate::before {
      content: '';
      position: absolute;
      top: 10px;
      left: 10px;
      right: 10px;
      bottom: 10px;
      border: 2px solid #004080;
    }
    
    .logo {
      width: 150px;
      margin-bottom: 30px;
    }
    
    .title {
      font-size: 36px;
      margin-bottom: 20px;
      color: #004080;
      text-transform: uppercase;
      letter-spacing: 2px;
    }
    
    .subtitle {
      font-size: 18px;
      margin-bottom: 40px;
      font-style: italic;
    }
    
    .recipient {
      font-size: 32px;
      margin: 40px 0;
      font-weight: bold;
      color: #004080;
    }
    
    .event-details {
      font-size: 20px;
      margin: 30px 0;
      line-height: 1.6;
    }
    
    .credits {
      font-size: 24px;
      margin: 30px 0;
      font-weight: bold;
      color: #004080;
    }
    
    .signatures {
      display: flex;
      justify-content: space-around;
      margin-top: 60px;
    }
    
    .signature {
      text-align: center;
      min-width: 200px;
    }
    
    .signature-line {
      border-top: 2px solid #333;
      margin-top: 50px;
      padding-top: 10px;
    }
    
    .certificate-footer {
      margin-top: 40px;
      font-size: 12px;
      color: #666;
    }
    
    .certificate-number {
      position: absolute;
      bottom: 20px;
      right: 40px;
      font-size: 10px;
      color: #999;
    }
  </style>
</head>
<body>
  <div class="certificate">
    <img src="/themes/custom/b5subtheme/images/logo.png" alt="Idaho Legal Aid Services" class="logo">
    
    <h1 class="title">{{ type }}</h1>
    
    <p class="subtitle">This certifies that</p>
    
    <h2 class="recipient">{{ contact.display_name }}</h2>
    
    <div class="event-details">
      <p>has successfully completed</p>
      <p><strong>{{ event.title }}</strong></p>
      <p>on {{ event.start_date|date('F j, Y') }}</p>
    </div>
    
    {% if credits %}
      <div class="credits">
        {{ credits }} CLE Credits Earned
      </div>
      {% if accreditation_number %}
        <p>Accreditation #{{ accreditation_number }}</p>
      {% endif %}
    {% elseif hours %}
      <div class="credits">
        {{ hours }} Training Hours Completed
      </div>
    {% endif %}
    
    <div class="signatures">
      <div class="signature">
        <div class="signature-line">
          <strong>Executive Director</strong><br>
          Idaho Legal Aid Services
        </div>
      </div>
      
      <div class="signature">
        <div class="signature-line">
          <strong>Training Coordinator</strong><br>
          Idaho Legal Aid Services
        </div>
      </div>
    </div>
    
    <div class="certificate-footer">
      <p>Idaho Legal Aid Services, Inc.<br>
      1447 S. Tyrell Lane | Boise, Idaho 83706<br>
      www.idaholegalaid.org</p>
    </div>
    
    <div class="certificate-number">
      Certificate #{{ certificate_number }}
    </div>
  </div>
</body>
</html>